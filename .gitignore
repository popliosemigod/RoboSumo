# ---- PlatformIO -----------------------------------------------------
.pio/
.pioenvs/
.piolibdeps/
.clang_complete
.gcc-flags.json
compile_commands.json

# ---- Arduino IDE ----------------------------------------------------
build/
*.hex
*.elf
*.bin
*.map
*.d
*.o

# ---- VSCode ---------------------------------------------------------
# Nada daqui e versionado. Os tres arquivos que ficavam de fora desta
# regra sairam na limpeza do repositorio, e manter as excecoes so fazia
# o editor recria-los e eles reaparecerem como nao rastreados.
.vscode/

# ---- Python (pre-commit / commitizen) -------------------------------
.venv/
venv/
__pycache__/
*.pyc

# ---- Notas de trabalho ----------------------------------------------
# Estado da bancada, pendencias e armadilhas ja encontradas. Fica fora do
# git de proposito: e caderno de sessao, muda toda hora e nao interessa a
# quem clona o repositorio. Recarregar no comeco de cada sessao.
CONTEXTO.md

# ---- Sistema --------------------------------------------------------
Thumbs.db
desktop.ini
.DS_Store
*.swp
*~

# ---- Diario de bancada -----------------------------------------------
# Registro corrido dos testes: e anotacao de laboratorio, muda a cada
# sessao e nao deve poluir o historico nem gerar conflito de merge.
DIARIO-BANCADA.md
